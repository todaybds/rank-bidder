"""SERP Measurer — AWS Lambda Seoul.

Story 1.4 LIVE: 모바일 SERP fetch + 다회 샘플링(FR-8, FR-10) +
X-Auth-Token(D9) + D13 응답 contract.

배포 단위는 ``template.yaml`` (SAM). Handler entrypoint = ``handler.lambda_handler``.
"""
