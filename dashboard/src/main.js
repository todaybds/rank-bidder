// Story 1.1 skeleton. Story 4.2가 실제 메인뷰 5요소 + 60s polling 로직 채움.
// Architecture step-05 §Patterns: vanilla JS + ES modules + named exports (no React/Next.js).

console.log("rank-bidder dashboard skeleton — Story 1.1");

const app = document.getElementById("app");
if (app) {
  app.textContent = "Story 1.1 skeleton — dashboard 구현은 Story 4.2~4.5에서 진행됩니다.";
}
